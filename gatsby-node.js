const Promise = require('bluebird');
const path = require('path');

//denne fikses når content-modellen er på plass
exports.createPages = ({ graphql, actions }) => {
    const { createPage } = actions;
};
