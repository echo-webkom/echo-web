version: '3.9'
services:
  backend:
    build: .
    image: echo-web-backend:latest
    depends_on:
      - database
    ports:
      - '5000:5000'
    environment:
      PORT: 5000

  database:
    image: postgres:13
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: postgres
