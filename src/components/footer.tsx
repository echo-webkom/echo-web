import React from 'react';
import { Image, Box, SimpleGrid, Heading } from '@chakra-ui/core';

const Footer = (): JSX.Element => {
    return <Heading as="h3">Halla</Heading>;
};

export default Footer;
